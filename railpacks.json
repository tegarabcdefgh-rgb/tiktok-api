{
  "$schema": "https://schema.railpack.com",
  "build": {
    "steps": [
      {
        "command": "apt-get update && apt-get install -y python3 python3-pip && pip3 install yt-dlp",
        "cache": true
      }
    ]
  },
  "deploy": {
    "aptPackages": ["ffmpeg"]
  }
}